import logging

_logger = logging.getLogger(__name__)

COMBINE_OPERATORS = ['&&', '||', '!']
COMPARISON_OPERATORS = ['<', '<=', '>', '>=', '==', '!=', 'like', 'not like']

def RedisSearchParser(obj):
    pass
